@model IEnumerable<WebApplication_UESL.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Student ID
        </th>
        <th>
            First Name
        </th>
        <th>
            Last Name
        </th>
        <th>
            Letter Grade
        </th>
        <th>
            Placement
        </th>
        <th>
            Quarter Of Placement
        </th>
        <th>
            Course Catalog Number
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StudentID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LetterGrade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Placement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.QuarterOfPlacement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CourseCatalogNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UniqueRecordID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UniqueRecordID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UniqueRecordID })
        </td>
    </tr>
}

</table>
